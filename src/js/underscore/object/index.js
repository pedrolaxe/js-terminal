import copyProperty from './copyProperty';
import decorateMethod from './decorateMethod';
import decorateMethods from './decorateMethods';
import extend from './extend';
import invoke from './invoke';

export default {
  copyProperty,
  decorateMethod,
  decorateMethods,
  extend,
  invoke,
};
